# A collection of utilities for decompiling and collating scripts and media assets
# from other systems.
#
# Indicate a ripper with the first arg, `detangl twine <src_dir> -o <dest_dir>` will
# invoke a ripper "rip_twine.py" if one can be found on the search path.
#
# Rippers should declare what root directory is required and what possible output
# items or formats it can provide.
