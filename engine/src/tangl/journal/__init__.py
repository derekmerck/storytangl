"""Journal fragment models and compatibility shims.

``tangl.journal.fragments`` is the canonical import surface for reusable
repo-owned fragment and hint types. Legacy subpackages remain as re-export
shims during the migration window.
"""
