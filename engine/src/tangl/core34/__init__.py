# todo: The core subpackage should support both feed-forward (runtime) and feed-backward (analysis, linting, export) flows.

# todo: include/promote to first class:
#    - provenance
#    - bidirectional traversal
#    - resolution traces
#    - export/linting APIs
