# affiliate domains are the default named Singleton domains that any node can opt into
# explicitly by naming them, or implicitly by being a member of an affiliated subscription,
# for example, an instance of a class that subscribes to a particular interaction handler
# domain.
